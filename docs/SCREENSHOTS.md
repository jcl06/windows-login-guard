# Lab Screenshots

These screenshots were extracted from the supplied v1.10.2 screen recording.

They show the real application running on a disposable lab VM. Visible
hostnames, usernames, QR payloads, TOTP setup keys, recovery codes, timestamps,
and audit values are lab-only examples.

They are intentionally not blurred or redacted.

Production recordings should have credentials, internal identifiers, and audit
data removed before publication.
